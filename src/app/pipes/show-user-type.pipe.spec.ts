import { ShowUserTypePipe } from './show-user-type.pipe';

describe('ShowUserTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowUserTypePipe();
    expect(pipe).toBeTruthy();
  });
});
